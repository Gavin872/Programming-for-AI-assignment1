[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "numcompute"
version = "0.1.0"
description = "A modular scientific computing toolkit built with plain Python and NumPy"
authors = [
    { name = "Your Team" }
]
dependencies = [
    "numpy",
    "pytest"
]